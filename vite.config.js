import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// Using React plugin via Vite's builtin support is optional; however, we keep plain JSX here.
export default defineConfig({
  plugins: [],
})
